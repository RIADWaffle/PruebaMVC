<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Salida de vehículo</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="card">
    <h1>Salida de vehículo</h1>
    <p>El vehículo con placa <%= vehicle.plateNumber %> ha salido.</p>
    <p>Tipo: <%= vehicle.type %></p>
    <p>Hora de entrada: <%= new Date(vehicle.entryTime).toLocaleString() %></p>
    <p>Hora de salida: <%= new Date(vehicle.exitTime).toLocaleString() %></p>
    <p>Duración en estacionamiento: <%= vehicle.getParkingDuration() %> minutos</p>
    <p>Paga: $<%= vehicle.getParkingFee() %></p>
    <a href="/">Volver al inicio</a>
  </div>
</body>
</html>
